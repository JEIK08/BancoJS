rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /Databases/{dbName}/{document=**} {
      allow read, write: if request.auth != null && request.auth.token.database == dbName;
    }
  }
}